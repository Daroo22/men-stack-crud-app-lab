<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Car</title>
  </head>
  <body>
    <h1>Edit <%= car.name %></h1>
    <form action="/cars/<%= car.id %>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" value="<%= car.name %>" />

        <label for="isReadyToDrive">Ready to Drive?</label>
        <input type="checkbox" id="isReadyToDrive" name="isReadyToDrive" <% if (car.isReadyToDrive) { %>checked<% } %> >

        <button type="submit">Update Car</button>
    </form>
    <a href="/cars">Back to Cars</a>
  </body>
</html>